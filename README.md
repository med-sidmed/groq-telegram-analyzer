# Telegram AI Analyzer

A Rust-based Telegram bot that uses AI to analyze content.

## Configuration

1. Copy `.env.example` to `.env`.
2. Fill in your `TELEGRAM_BOT_TOKEN` and `OPENAI_API_KEY`.
3. Run with `cargo run`.
